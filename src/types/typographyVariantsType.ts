export type TypographyVariantsType = {
  title: {
    fontSize: string;
    fontWeight: string;
    lineHeight: number;
  };
  titleXS: {
    fontSize: string;
    fontWeight: string;
    lineHeight: number;
  };
  subTitle: {
    fontSize: string;
    fontWeight: string;
    lineHeight: number;
  };
  paragraph: {
    fontSize: string;
    fontWeight: string;
    lineHeight: number;
  };
};
